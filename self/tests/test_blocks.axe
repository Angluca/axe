macro add(x: untyped, y: untyped) {
    def some_function() {
        return x + y;
    }
}

def some_other_function(): i32 {
    return 1;
}

add(1, 2);

test {
    val x: i32 = 1;
    assert x == 1, "Should be true.";
    unsafe {
        C.printf("%d", some_other_function());
    }
}
