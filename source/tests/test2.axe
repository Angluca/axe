use std.string;
use std.io;

model Person {
    name: string;
    age: i32;
}

main {
    parallel for mut i = 0 to 10 {
        mut person = new Person(name: "Alice", age: i);
        print_str(str($"Hello {person.age}"));
    }
}
