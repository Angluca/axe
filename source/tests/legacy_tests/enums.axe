use std.io;

enum State {
    RUNNING,
    CHANGING,
    STOPPED
}

def main() {
    mut val fixed_state: State = State.CHANGING;
    println cast[i32](State.RUNNING);
    println cast[i32](fixed_state);
    fixed_state = State.RUNNING;
}
