use std.io;

[inline]
def some_func[T](arg: T): T {
    when T is i32 {
        return arg + 1;
    }
    when T is f32 {
        return arg * 2.0;
    }
}

def some_other_func(x: i32) {
    println(x);
}

def main() {
    println(some_func(5));
}
